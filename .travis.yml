dist: trusty
sudo: require

language: cpp
os: linux
compiler: gcc
  
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - gcc-6
            - g++-6
            - cmake

script:
    #Test running script
    return 0;

after_script:
    #deploy default Message

after_success:
    #deploy Success message
    - echo "success"

after_failure:
    - echo "failure"
